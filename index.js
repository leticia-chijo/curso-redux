// State ------------------------------------------------------
const initialStateTitle = "";
const initialStateTasks = [];

function addItemToList(task) {
  initialStateTasks.push(task);
}

addItemToList("Lavar louça")
addItemToList("Estudar redux")
